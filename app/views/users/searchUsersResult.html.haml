- if @users.count > 0
  %ul
    - for user in @users
      %li{:class => ("inactive" unless user.active?) }
        = link_to user.name, user
    - if @users.count > 5
      %li
        + 
        = @users.count - 5 
        more users
    
- else
  %p No Users Found